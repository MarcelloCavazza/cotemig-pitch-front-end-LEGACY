import styled from 'styled-components';
import colors from '../../../global-styles/colors';

export const ContactName = styled.span`
  font-size: 13pt;
  color: white;
`

export const ContactLastMessage = styled.span`
  font-size: 11pt;
  color: ${colors.chatroomTextOnRead};
  font-weight: 400;
`